import ScanClient from './ScanClient';

export const metadata = {
  title: 'Pit Scan — Catalyst Pit',
  description: 'What is moving, and what the public evidence says about it.',
};

export default function ScanPage() {
  return <ScanClient />;
}
