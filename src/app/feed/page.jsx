import FeedClient from './FeedClient';

export const metadata = {
  title: 'Feed — CatalystPit',
  description: 'The community feed — traders sharing thoughts, ideas, and catalysts.',
};

export default function FeedPage() {
  return <FeedClient />;
}
